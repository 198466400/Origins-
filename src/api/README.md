# ORIGIN — Public API Layer

The Public API exposes ORIGIN to external systems in a safe, controlled way.

It provides:
- start/stop controls
- one-off cycle execution
- access to last cycle summary
- system status

The API does NOT expose internal subsystem details.
It only exposes high-level, safe summaries.

This ensures ORIGIN remains introspective, autonomous, and user-sovereign.
